# Comparison Report

The summaries generated by the manual parser and the pandas parser are identical in nature..

Both implementations produced identical results for total students, submission statistics, average score, highest and lowest scorers, domain-wise averages, and student lists.

This identicality occurred because both implementations process the same CSV file, apply the same type conversions, and perform the same calculations and implement the same functionality.
